version: '3.1'

services:
  db:
    image: mariadb:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
      MYSQL_DATABASE: 'mentor'
      MYSQL_USER: 'user'
      MYSQL_PASSWORD: password
    volumes:
      - ../data:/var/lib
    ports:
      - '3306:3306'
